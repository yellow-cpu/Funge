'use strict';

describe('master', function() {

  // Load the module that contains the `master` component before each test
  beforeEach(module('master'));

  // Test the controller
  describe('MasterController', function() {

    it('should xxx', inject(function($componentController) {
      var ctrl = $componentController('master');
    }));

  });

});
