'use strict';

describe('plants', function() {

  // Load the module that contains the `plants` component before each test
  beforeEach(module('plants'));

  // Test the controller
  describe('PlantsController', function() {

    it('should xxx', inject(function($componentController) {
      var ctrl = $componentController('plants');
    }));

  });

});
