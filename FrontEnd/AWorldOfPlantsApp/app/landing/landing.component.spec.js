'use strict';

describe('landing', function() {

  // Load the module that contains the `landing` component before each test
  beforeEach(module('landing'));

  // Test the controller
  describe('LandingController', function() {

    it('should xxx', inject(function($componentController) {
      var ctrl = $componentController('landing');
    }));

  });

});
