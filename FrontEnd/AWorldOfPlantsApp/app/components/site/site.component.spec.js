'use strict';

describe('site', function() {

  // Load the module that contains the `site` component before each test
  beforeEach(module('site'));

  // Test the controller
  describe('SiteController', function() {

    it('should xxx', inject(function($componentController) {
      var ctrl = $componentController('site');
    }));

  });

});
