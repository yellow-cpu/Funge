'use strict';

describe('createPlant', function() {

  // Load the module that contains the `createPlant` component before each test
  beforeEach(module('createPlant'));

  // Test the controller
  describe('CreatePlantController', function() {

    it('should xxx', inject(function($componentController) {
      var ctrl = $componentController('createPlant');
    }));

  });

});
