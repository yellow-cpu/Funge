'use strict';

// Register `profile` directive
angular.
module('profile').
directive('profileDirective', function ($compile) {
  var linkFunction = function (scope, element, attributes) {

  };

  return {
    link: linkFunction
  };
});
