'use strict';

describe('profile', function() {

  // Load the module that contains the `profile` component before each test
  beforeEach(module('profile'));

  // Test the controller
  describe('ProfileController', function() {

    it('should xxx', inject(function($componentController) {
      var ctrl = $componentController('profile');
    }));

  });

});
