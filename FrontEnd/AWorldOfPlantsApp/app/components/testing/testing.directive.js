'use strict';

// Register `testing` directive
angular.
module('testing').
directive('testing', function($compile) {
    var linkFunction = function(scope, element, attributes) {
        
    };

    return {
        link: linkFunction
    };
});
